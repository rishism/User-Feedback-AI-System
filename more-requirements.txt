Read and understand @my_project.md.  I want to create a plan to implement this project.
Apart from the instructions in the markdown file, also Here are newer/updated project instructions. These instructions are more recent than the contents
in the markdown file. So the instructions in the following text should override/prevail if there is any conflict when compared to the instructions in
markdown file. Also, do not refer to any sibling folders in my machine to find out any other information; just use the inputs that I am providing.

### 🎯 Project Overview
*   **Main Objective:** Build a system to handle and process incoming customer queries and feedback.
*   **Classification Categories:** The system must classify inputs into specific categories: bug, feature request, praise, complaint, or spam.

### 🤖 Required Agents & Frameworks
*   **Agent Roles Needed:**
    *   CSV Agent
    *   Feedback Classifier
    *   Bug Analyzer
    *   Feature Extractor
    *   Ticket Creator
    *   Quality Critic Agent
*   **Framework:** You will build this multi-agent setup using **LangChain** and **LangGraph**.

### 🛠️ Implementation Steps
*   **Agent Definition:** Implement specific agent classes, define their roles, and give each agent its own specific system prompt.
*   **Pipeline Setup:** Structure how data is passed from one agent to another using a LangGraph pipeline.
*   **Classification Method:** Do not build a new machine-learning model. Instead, use an LLM by prompting it to classify the text into the required
categories and return the corresponding scores.
*   **Ticket Generation:** You can simulate Jira's ticket creation by creating an MCP server that connects to an **SQLite database** and building a
"create ticket" tool.

### 💾 Data Handling & User Interface
*   **Data Sources:** The system will use two different data sources. Mock information is provided, and you can use ChatGPT to generate additional mock
records.
*   **User Interface (UI):** Build a Streamlit UI where users can upload a CSV file or submit reviews or submit feedback requests.
*   **Data Processing:** Write Python code to process the CSV files and store the data into an **SQLite database** table (ensure your database has the
right structure set up for direct CSV imports).

### 📊 Observability & Deliverables
*   **Observability/Analytics:** This is a strict requirement. The instructor recommends using **Langfuse** for this purpose.
*   **Documentation:** You must include a **README file** that details any specific scripts or commands needed to run your unique project setup.

Tell me your understanding. Use the ask user question tool if required.